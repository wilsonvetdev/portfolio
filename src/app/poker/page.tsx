import Lobby from "@/components/poker/Lobby";

export const metadata = {
  title: "Planning Poker | Wilson The Vet Dev",
  description: "Real-time planning poker for agile teams",
};

export default function PokerPage() {
  return <Lobby />;
}
